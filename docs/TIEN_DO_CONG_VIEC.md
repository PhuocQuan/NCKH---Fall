# Bảng tiến độ công việc — NCKH Fall Detection

**Người thực hiện chính:** Bảo  
**Cập nhật:** 11/06/2026  
**Kiểm thử hiện tại:** `75 passed` (`pytest`)

> Copy bảng dưới vào Excel/Google Sheets (cột phân tách bằng tab nếu cần — xem file `.tsv` cùng thư mục hoặc bảng markdown).

---

| STT | TÊN CÔNG VIỆC | CHI TIẾT CÔNG VIỆC | NGƯỜI THỰC HIỆN | NGÀY BẮT ĐẦU - KẾT THÚC | LUỒNG HOẠT ĐỘNG | NGÔN NGỮ LẬP TRÌNH | TIẾN ĐỘ CÔNG VIỆC |
|-----|---------------|-------------------|------------------|-------------------------|------------------|---------------------|-------------------|
| 1 | Tạo app desktop | 1. Tạo `desktop_app.py` — giao diện Tkinter<br>2. Thêm nút Start / Stop / Reset điều khiển demo<br>3. Hiển thị luồng camera/video, trạng thái, FPS và event log | Bảo | 9/6/2026 - 9/6/2026 | Khởi tạo giao diện → Người dùng bấm Start/Stop → Hiển thị video + trạng thái + log trên màn hình | Python (Tkinter) | Đã hoàn thành |
| 2 | Liên kết app với dự án | 1. App gọi `VideoSource` đọc webcam hoặc file video<br>2. Dùng `PoseEstimator`, `FallDetector`, `EventLogger` từ pipeline hiện có<br>3. Đọc cấu hình từ `configs/default.yaml` khi chạy | Bảo | 9/6/2026 - 9/6/2026 | Chọn nguồn camera/video → Đọc frame → Pose → Detector → Ghi log; config load từ YAML khi mở app | Python, YAML | Đã hoàn thành |
| 3 | Chuẩn bị nền tảng AI | 1. Thêm cấu hình `ai.decision_mode`, `assist_min_frames` trong YAML<br>2. Thêm script `evaluate_videos.py` đánh giá trên video gán nhãn<br>3. Thêm `train_ai_model.py` train model và xuất report metric | Bảo | 9/6/2026 - 9/6/2026 | Video gán nhãn → Trích feature → Train model → Đánh giá metric; cấu hình AI trong YAML (tắt mặc định) | Python, YAML, scikit-learn | Đã hoàn thành |
| 4 | Tối ưu demo phát hiện té ngã | 1. Gom xử lý phát hiện té ngã dùng chung cho terminal (`app.py`) và desktop<br>2. Hiển thị FPS, reset ổn định, ghi log kèm nguồn camera và FPS<br>3. Vẫn chạy được khi chưa train model AI | Bảo | 9/6/2026 - 9/6/2026 | Frame → Pose → FallDetector → Overlay + Log; CLI và desktop dùng chung luồng xử lý, không phụ thuộc model AI | Python, OpenCV | Đã hoàn thành |
| 5 | Tối ưu app desktop | 1. Thêm nút chọn Webcam 0/1, chọn video, chọn file config<br>2. Thêm mô tả nguồn camera/config và đổi màu theo trạng thái<br>3. Cải thiện trải nghiệm demo trên giao diện desktop | Bảo | 9/6/2026 - 9/6/2026 | Chọn nguồn (webcam/video/config) → Cập nhật mô tả nguồn → Đổi màu UI theo trạng thái detector | Python (Tkinter) | Đã hoàn thành |
| 6 | Refactor pipeline chung | 1. Tạo `src/pipeline.py` gom xử lý frame<br>2. Tạo `PipelineResult` trả về trạng thái, FPS, pose_ms, event<br>3. Refactor `app.py` và `desktop_app.py` dùng chung pipeline | Bảo | 10/6/2026 - 11/6/2026 | VideoSource → `process_frame()` → PipelineResult (state, FPS, pose_ms, event) → CLI/desktop chỉ render kết quả | Python | Đã hoàn thành |
| 7 | Module overlay & cảnh báo | 1. Tạo `src/ui_overlay.py` — vẽ trạng thái, màu, khung cảnh báo<br>2. Tạo `src/alert_sound.py` — beep Windows + `FallAlarmTracker`<br>3. Bỏ code vẽ/cảnh báo trùng giữa CLI và desktop | Bảo | 10/6/2026 - 11/6/2026 | Detector trả state → `ui_overlay` vẽ khung/màu → `FallAlarmTracker` kích beep khi ALERT | Python, OpenCV | Đã hoàn thành |
| 8 | Grace period mất pose | 1. Thêm `max_pose_lost_frames: 15` trong config<br>2. Giữ trạng thái detector khi mất pose tạm thời (~0.5s)<br>3. Hiển thị TRACKING, CHO POSE thay vì reset ngay | Bảo | 10/6/2026 - 11/6/2026 | Mất landmark tạm thời → Đếm frame mất pose → Giữ state cũ / hiện CHO POSE → Reset chỉ khi vượt ngưỡng | Python, YAML | Đã hoàn thành |
| 9 | Kiểm thử & pytest | 1. Thêm `test_pipeline.py`, `test_alert_sound.py`<br>2. Thêm `pytest.ini` + thư mục `.tmp/` (fix lỗi Windows)<br>3. Tăng từ 22 lên 37 test pass | Bảo | 10/6/2026 - 11/6/2026 | Viết test → Chạy pytest → Kiểm tra pipeline, alert, detector, settings pass trên Windows | Python (pytest) | Đã hoàn thành |
| 10 | Tối ưu hiệu năng Pose | 1. Thêm `PoseConfig` — resize 640×360 trước khi chạy MediaPipe<br>2. Đặt `model_complexity: 0` cho demo nhanh<br>3. Đo và hiển thị `pose_ms` mỗi frame | Bảo | 10/6/2026 - 11/6/2026 | Resize frame → MediaPipe Pose → Đo pose_ms → Trả landmark; cấu hình trong `configs/default.yaml` | Python, MediaPipe, YAML | Đã hoàn thành |
| 11 | Quét camera & hiệu năng | 1. Thêm `scan_cameras()` + dropdown chọn webcam<br>2. Nút Quét — tự tìm camera khi mở app<br>3. Khung Hiệu năng: FPS, Pose ms, input size, model | Bảo | 10/6/2026 - 11/6/2026 | Mở app → Quét index camera → Chọn webcam → Hiển thị FPS/pose_ms/input size trên panel | Python, OpenCV | Đã hoàn thành |
| 12 | Panel Cài đặt | 1. Tạo `user_settings.py` — load/save `configs/user.yaml`<br>2. UI: Profile, cảnh báo sau (giây), skeleton, snapshot<br>3. Áp dụng settings khi Start (không cần sửa YAML thủ công) | Bảo | 10/6/2026 - 11/6/2026 | Chỉnh tab Cài đặt → Lưu `user.yaml` → Bấm Start → Pipeline áp dụng profile/ngưỡng/skeleton/snapshot | Python, YAML (Tkinter) | Đã hoàn thành |
| 13 | Snapshot & progress bar | 1. Tạo `snapshot.py` — lưu ảnh khi alert vào `data/snapshots/`<br>2. Thanh progress đếm `lying_seconds` / `alert_after_seconds`<br>3. Ghi tên ảnh vào event log | Bảo | 10/6/2026 - 11/6/2026 | State FALLEN/ALERT → Cập nhật progress bar → Khi ALERT lưu ảnh JPG → Ghi tên file vào CSV log | Python, OpenCV | Đã hoàn thành |
| 14 | Event log nâng cao | 1. Thêm `EventLogger.read_recent()` — đọc CSV<br>2. Tự load 12–15 sự kiện gần nhất khi mở app<br>3. Nút Tải lại / Xóa / Mở CSV | Bảo | 10/6/2026 - 11/6/2026 | Mở app → Đọc `events.csv` → Hiển thị log tab Nhật ký → Tải lại/Xóa/Mở file CSV | Python, CSV | Đã hoàn thành |
| 15 | Tối ưu giao diện desktop | 1. Chia 3 tab: Giám sát / Cài đặt / Nhật ký<br>2. Paned window — kéo giãn vùng camera<br>3. Placeholder video, phím Space (Start/Stop), R (Reset) | Bảo | 10/6/2026 - 11/6/2026 | Tab Giám sát (video + điều khiển) ↔ Cài đặt ↔ Nhật ký; Space/R điều khiển nhanh không cần chuột | Python (Tkinter) | Đã hoàn thành |
| 16 | Tối ưu hiệu năng UI | 1. Tạo `desktop_view.py` — `FramePresenter`, `UiRefreshGate`<br>2. Giới hạn render video 30 FPS<br>3. Throttle cập nhật sidebar ~120ms; loop `after(10)` | Bảo | 10/6/2026 - 11/6/2026 | Frame mới → FramePresenter giới hạn 30 FPS → UiRefreshGate throttle sidebar → `after(10)` loop mượt | Python (Tkinter) | Đã hoàn thành |
| 17 | EventLogger session | 1. Thêm `open_session()` / `close_session()`<br>2. Giữ file CSV mở trong phiên chạy<br>3. Pipeline gọi session khi Start/Stop | Bảo | 10/6/2026 - 11/6/2026 | Start → `open_session()` mở CSV → Ghi event trong phiên → Stop → `close_session()` đóng file | Python, CSV | Đã hoàn thành |
| 18 | Cải thiện UX & nhãn | 1. Trạng thái tiếng Việt: Sẵn sàng, Chờ nhận diện, CẢNH BÁO…<br>2. Profile có tên: elderly - Người già…<br>3. Ẩn config YAML trong mục “Nâng cao” | Bảo | 10/6/2026 - 11/6/2026 | State machine → Map sang nhãn tiếng Việt trên UI → Profile hiển thị tên dễ hiểu cho người dùng cuối | Python (Tkinter) | Đã hoàn thành |
| 19 | Rà soát & sửa lỗi | 1. Sửa `evaluate_videos.py` — API Pose mới (tránh crash)<br>2. Sửa `build_feature_dataset.py` — cùng lỗi + thêm `--config`<br>3. Sửa `fall_detector.py` — trạng thái WARNING không bao giờ kích hoạt | Bảo | 10/6/2026 - 11/6/2026 | Chạy pytest + script ML → Phát hiện lỗi API Pose / logic WARNING → Sửa → 37 test pass | Python (pytest) | Đã hoàn thành |
| 20 | API server mobile | 1. Tạo `src/api_server.py` — FastAPI, CORS, phục vụ web tĩnh<br>2. Tạo `src/mobile_service.py` — pipeline chạy nền, MJPEG stream<br>3. API: status, control, events, settings, snapshot, WebSocket `/ws/status` | Bảo | 10/6/2026 - 11/6/2026 | Client gọi REST/WS → `MobileMonitorService` chạy pipeline → Trả JSON status + stream `/api/camera/stream.mjpg` | Python (FastAPI, Uvicorn) | Đã hoàn thành |
| 21 | Xác thực & phiên đăng nhập | 1. Tạo `src/auth.py` — token Bearer, admin + users trong `auth.yaml`<br>2. API: `/api/auth/login`, `/api/auth/me`, `/api/auth/logout`<br>3. Chế độ `admin_provisioned` — không tự đăng ký | Bảo | 10/6/2026 - 11/6/2026 | Login → Verify user → Cấp token → Mọi API bảo vệ bằng `Authorization: Bearer` | Python, YAML | Đã hoàn thành |
| 22 | Giao diện Guardian Watch | 1. Thiết kế `mobile/web/` — splash, login, 4 tab chính<br>2. Home: banner cảnh báo, thống kê, hoạt động gần đây<br>3. Màn hình khẩn cấp full-screen khi `alert` | Bảo | 10/6/2026 - 11/6/2026 | Mở app → Login → Tab Home/Camera/Cảnh báo/Hồ sơ → WebSocket cập nhật UI realtime | HTML, CSS, JavaScript | Đã hoàn thành |
| 23 | Quản lý camera (admin) | 1. Tạo `src/camera_registry.py` — CRUD `configs/cameras.yaml`<br>2. UI thêm/sửa/xóa camera; nguồn webcam (`0`) / RTSP / file<br>3. Mặc định **không có camera** — admin tự thêm | Bảo | 10/6/2026 - 11/6/2026 | Admin thêm camera → Lưu YAML → User chọn camera → Start giám sát theo `camera_id` | Python, YAML, JavaScript | Đã hoàn thành |
| 24 | Yêu cầu truy cập | 1. Tạo `src/access_requests.py` — lưu `data/access_requests.json`<br>2. Form công khai trên màn login<br>3. Admin duyệt/từ chối trong app | Bảo | 10/6/2026 - 11/6/2026 | User gửi yêu cầu → Lưu JSON → Admin xem danh sách → Duyệt/Từ chối → Tạo tài khoản thủ công | Python, JavaScript, JSON | Đã hoàn thành |
| 25 | Luồng kết nối mobile | 1. Card “Kiểm tra kết nối” + nhập IP server<br>2. Tab Cài đặt: Lưu & kết nối lại server<br>3. Server in IP LAN khi khởi động (`0.0.0.0:8000`) | Bảo | 10/6/2026 - 11/6/2026 | Nhập `http://IP:8000` → Health check → Login → WebSocket + stream hoạt động trên cùng WiFi | Python, JavaScript | Đã hoàn thành |
| 26 | APK Android (Capacitor) | 1. Tạo `mobile/android-app/` — Capacitor 7, `npm run cap:sync`<br>2. `docs/BUILD_APK.md` — hướng dẫn Android Studio<br>3. `network_security_config.xml` cho HTTP cleartext | Bảo | 10/6/2026 - 11/6/2026 | Sửa `mobile/web` → `cap:sync` → Build APK → Cài điện thoại → Kết nối server laptop | JavaScript, Java (Android), Gradle | Đã hoàn thành |
| 27 | Cảnh báo âm thanh mobile | 1. File chuông `mobile/web/sounds/fall_alert.wav`<br>2. `playFallAlertSound()` + rung (`navigator.vibrate`)<br>3. Nút “Thử chuông” trong Cài đặt; server/desktop dùng chung WAV | Bảo | 10/6/2026 - 11/6/2026 | State POSSIBLE_FALL/FALLEN/ALERT + đang giám sát → Phát chuông lặp ~2s → Tắt khi bình thường hoặc Stop | JavaScript, Python (`winsound`) | Đã hoàn thành |
| 28 | Phân quyền admin | 1. Chỉ admin CRUD camera và quản lý user<br>2. User thường: xem camera, giám sát, cài đặt cá nhân<br>3. UI ẩn nút + Thêm / ✏️ theo `is_admin` | Bảo | 10/6/2026 - 11/6/2026 | Login → API trả `is_admin` → UI hiện/ẩn menu admin → API trả 403 nếu vi phạm | Python, JavaScript | Đã hoàn thành |
| 29 | Quản lý người dùng | 1. API `/api/users` — CRUD, lưu `configs/auth.yaml`<br>2. Trang **Hồ sơ → Quản lý người dùng**<br>3. Tạo/sửa/khóa/xóa user; không xóa admin | Bảo | 10/6/2026 - 11/6/2026 | Admin mở danh sách user → Thêm/Sửa → Ghi `auth.yaml` → User đăng nhập bằng tài khoản mới | Python, YAML, JavaScript | Đã hoàn thành |
| 30 | Sửa lỗi camera & UI mobile | 1. Sửa thêm 1 camera ra nhiều bản — lock YAML + chặn submit trùng<br>2. Sửa `Cannot set properties of null` — helper `txt()`/`val()`<br>3. Đồng bộ HTML ↔ JS; `cap:sync` trước build APK | Bảo | 10/6/2026 - 11/6/2026 | Phát hiện lỗi khi thao tác app → Sửa client + server → Chạy lại pytest → Sync web sang Android | Python, JavaScript | Đã hoàn thành |
| 31 | Kiểm thử mobile API | 1. Thêm `test_api_server.py`, `test_auth.py`, `test_camera_registry.py`<br>2. Thêm `test_access_requests.py`, `test_mobile_service.py`, `test_ui_theme.py`<br>3. Tổng **75 test pass** (từ 37) | Bảo | 10/6/2026 - 11/6/2026 | Viết test API/auth/camera → `pytest` → Đảm bảo mobile backend ổn định trên Windows | Python (pytest) | Đã hoàn thành |
| 32 | Tài liệu & theme mobile | 1. Tạo `src/ui_theme.py` — màu/nhãn trạng thái cho mobile<br>2. Cập nhật `docs/MOBILE_APP.md`, `docs/BUILD_APK.md`<br>3. `manifest.json`, icon PWA/APK | Bảo | 10/6/2026 - 11/6/2026 | Định nghĩa theme API → Mobile UI đọc `/api/ui-config` → Hiển thị nhãn/màu thống nhất | Python, JavaScript, Markdown | Đã hoàn thành |
| 33 | Gom nhánh & release | 1. Merge nhánh `feature/mobile-api` → `main`<br>2. Commit đầy đủ `mobile/`, API, configs example<br>3. Tag phiên bản demo (v1.1 mobile) | Bảo | — | Code review → Merge → Push GitHub → Đồng bộ tài liệu README | Git | Chưa hoàn thành |
| 34 | G5 — Bộ video & đánh giá | 1. Thu thập video `data/videos/` (fall, non_fall, sleeping)<br>2. Chạy `evaluate_videos.py`, ghi metric CSV<br>3. Train AI khi đủ dữ liệu; bật `ai.enabled` | Bảo | — | Video gán nhãn → Feature → Train → So sánh rule vs rule+AI | Python, scikit-learn | Chưa bắt đầu |
| 35 | G6 — Báo cáo & demo bảo vệ | 1. Slide + báo cáo NCKH (bảng tiến độ này)<br>2. Video demo: desktop + mobile + APK + cảnh báo chuông<br>3. Kịch bản demo trước hội đồng | Bảo | — | Chuẩn bị slide → Quay demo → Tổng hợp báo cáo → Bảo vệ | Markdown, PowerPoint | Chưa bắt đầu |

---

## Tóm tắt theo giai đoạn

| Giai đoạn | Nội dung | STT | Trạng thái |
|-----------|----------|-----|------------|
| G1–G2 | Core pipeline, detector, CLI | 1–5 | Hoàn thành |
| G3–G4 | Desktop app, tối ưu, settings, test | 6–19 | Hoàn thành |
| G5 (mobile) | API, Guardian Watch, APK, auth, camera, user | 20–32 | Hoàn thành |
| G5 (AI data) | Video gán nhãn, evaluate, train | 34 | Chưa bắt đầu |
| G6 | Báo cáo, slide, demo | 33, 35 | Chưa hoàn thành |
