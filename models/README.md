# Thu muc model

Thu muc nay chua model AI sau khi train.

Model mac dinh ma app doc:

```text
models/fall_classifier.joblib
```

Sau khi train xong, bat AI trong `configs/default.yaml` bang cach dat `ai.enabled: true`.
